import React from 'react';
import { render } from '@testing-library/react';

import CenterPage from './CenterPage';

describe('CenterPage', () => {
  it('renders CenterPage', () => {
    render(<CenterPage />);
  });
});